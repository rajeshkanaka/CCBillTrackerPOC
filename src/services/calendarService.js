export async function setReminder(date, description) {
  // This is a mock function. In a real application, you would
  // use the Google Calendar API to create an event with a reminder.
  console.log(`Setting reminder for ${date}: ${description}`)
}
